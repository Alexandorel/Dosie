# Server
PORT=3000

# PostgreSQL
DATABASE_URL=postgresql://dosie:dosie_dev@localhost:5433/dosie

# Redis
REDIS_URL=redis://localhost:6379

# Auth — generate a secret with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Twilio (voice gateway)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# OpenAI (AI engine)
OPENAI_API_KEY=
